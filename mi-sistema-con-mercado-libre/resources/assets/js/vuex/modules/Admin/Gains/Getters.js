export const GainsFromDateGetter = (state) => state.gains.from;

export const GainsToDateGetter = (state) => state.gains.to;

export const GainsListGetter = (state) => state.gains.list;

export const GainsPaginationGetter = (state) => state.gains.pagination;

export const GainsTotalEarningsGetter = (state) => state.gains.total;